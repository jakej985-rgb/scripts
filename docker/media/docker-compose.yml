x-logging: &default-logging
  driver: "json-file"
  options:
    max-size: "50m"
    max-file: "3"

services:
  qbittorrent:
    image: lscr.io/linuxserver/qbittorrent
    container_name: qbittorrent
    network_mode: "container:gluetun"
    environment:
      - PUID=1000
      - PGID=1000
      - WEBUI_PORT=8080
    volumes:
      - ${CONFIG_DIR:?CONFIG_DIR not set — run from repo root with .env}/qbittorrent:/config
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}:/mnt
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/downloads:/downloads
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/media:/media
    env_file:
      - ../../.env
    labels:
      - "m3tal.stack=media"
    restart: unless-stopped

  radarr:
    image: lscr.io/linuxserver/radarr
    container_name: radarr
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.radarr.rule=Host(`radarr.m3tal-media-server.xyz`)"
      - "traefik.http.services.radarr.loadbalancer.server.port=7878"
      - "traefik.http.services.radarr.loadbalancer.healthcheck.path=/api/v3/system/status"
      - "traefik.http.services.radarr.loadbalancer.healthcheck.interval=10s"
      - "traefik.http.services.radarr.loadbalancer.sticky.cookie=true"
      - "m3tal.stack=media"
    networks:
      - proxy
    env_file:
      - ../../.env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7878"]
      interval: 30s
      timeout: 10s
      retries: 3
    volumes:
      - ${CONFIG_DIR:?CONFIG_DIR not set — run from repo root with .env}/radarr:/config
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}:/mnt
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/downloads:/downloads
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/media:/media
    restart: unless-stopped

  sonarr:
    image: lscr.io/linuxserver/sonarr
    container_name: sonarr
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.sonarr.rule=Host(`sonarr.m3tal-media-server.xyz`)"
      - "traefik.http.services.sonarr.loadbalancer.server.port=8989"
      - "traefik.http.services.sonarr.loadbalancer.healthcheck.path=/api/v3/system/status"
      - "traefik.http.services.sonarr.loadbalancer.healthcheck.interval=10s"
      - "traefik.http.services.sonarr.loadbalancer.sticky.cookie=true"
      - "m3tal.stack=media"
    networks:
      - proxy
    env_file:
      - ../../.env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8989"]
      interval: 30s
      timeout: 10s
      retries: 3
    volumes:
      - ${CONFIG_DIR:?CONFIG_DIR not set — run from repo root with .env}/sonarr:/config
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}:/mnt
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/downloads:/downloads
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/media:/media

    restart: unless-stopped

  bazarr:
    image: lscr.io/linuxserver/bazarr:latest
    container_name: bazarr
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6767"]
      interval: 60s
      timeout: 10s
      retries: 3
    volumes:
      - ${CONFIG_DIR:?CONFIG_DIR not set — run from repo root with .env}/bazarr:/config
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}:/mnt
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/downloads:/downloads
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/media:/media
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.bazarr.rule=Host(`bazarr.${DOMAIN:?DOMAIN not set — run from repo root with .env}`)"
      - "traefik.http.services.bazarr.loadbalancer.server.port=6767"
      - "m3tal.stack=media"
    networks:
      - proxy
    env_file:
      - ../../.env
    restart: unless-stopped

  komga:
    image: gotson/komga
    container_name: komga
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:25600/actuator/health"]
      interval: 60s
      timeout: 10s
      retries: 3
    volumes:
      - ${CONFIG_DIR:?CONFIG_DIR not set — run from repo root with .env}/komga:/config
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}:/mnt
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/downloads:/downloads
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/media:/media
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.komga.rule=Host(`komga.${DOMAIN:?DOMAIN not set — run from repo root with .env}`)"
      - "traefik.http.services.komga.loadbalancer.server.port=25600"
      - "m3tal.stack=media"
    networks:
      - proxy
    env_file:
      - ../../.env
    restart: unless-stopped

  jellyseerr:
    image: fallenbagel/jellyseerr
    container_name: jellyseerr
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5055"]
      interval: 60s
      timeout: 10s
      retries: 3
    volumes:
      - ${CONFIG_DIR:?CONFIG_DIR not set — run from repo root with .env}/jellyseerr:/app/config
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.jellyseerr.rule=Host(`jellyseerr.${DOMAIN:?DOMAIN not set — run from repo root with .env}`)"
      - "traefik.http.services.jellyseerr.loadbalancer.server.port=5055"
      - "m3tal.stack=media"
    networks:
      - proxy
    env_file:
      - ../../.env
    restart: unless-stopped

  homepage:
    image: ghcr.io/gethomepage/homepage:latest
    container_name: homepage
    volumes:
      - ${CONFIG_DIR:?CONFIG_DIR not set — run from repo root with .env}/homepage:/app/config
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.homepage.rule=Host(`home.${DOMAIN:?DOMAIN not set — run from repo root with .env}`)"
      - "traefik.http.services.homepage.loadbalancer.server.port=3000"
      - "m3tal.stack=media"
    networks:
      - proxy
    env_file:
      - ../../.env
    restart: unless-stopped

  autobrr:
    image: ghcr.io/autobrr/autobrr:v1.39
    container_name: autobrr
    volumes:
      - ${CONFIG_DIR:?CONFIG_DIR not set — run from repo root with .env}/autobrr:/config
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}:/mnt
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/downloads:/downloads
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/media:/media
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.autobrr.rule=Host(`autobrr.${DOMAIN:?DOMAIN not set — run from repo root with .env}`)"
      - "traefik.http.services.autobrr.loadbalancer.server.port=8080"
      - "m3tal.stack=media"
    networks:
      - proxy
    env_file:
      - ../../.env
    restart: unless-stopped

  recyclarr:
    image: ghcr.io/recyclarr/recyclarr
    container_name: recyclarr
    volumes:
      - ${CONFIG_DIR:?CONFIG_DIR not set — run from repo root with .env}/recyclarr:/config
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}:/mnt
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/downloads:/downloads
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/media:/media
    labels:
      - "m3tal.stack=media"
    restart: unless-stopped

  maintainerr:
    image: ghcr.io/maintainerr/maintainerr:latest
    container_name: maintainerr
    volumes:
      - ${CONFIG_DIR:?CONFIG_DIR not set — run from repo root with .env}/maintainerr:/opt/data
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.maintainerr.rule=Host(`maintainerr.${DOMAIN:?DOMAIN not set — run from repo root with .env}`)"
      - "traefik.http.services.maintainerr.loadbalancer.server.port=6246"
      - "m3tal.stack=media"
    networks:
      - proxy
    env_file:
      - ../../.env
    restart: unless-stopped

  tdarr:
    image: ghcr.io/haveagitgat/tdarr:latest
    container_name: tdarr
    volumes:
      - ${CONFIG_DIR:?CONFIG_DIR not set — run from repo root with .env}/tdarr:/app/config
      - ${CONFIG_DIR:?CONFIG_DIR not set — run from repo root with .env}/tdarr:/app/logs
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}:/mnt
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/downloads:/downloads
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/media:/media
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.tdarr.rule=Host(`tdarr.${DOMAIN:?DOMAIN not set — run from repo root with .env}`)"
      - "traefik.http.services.tdarr.loadbalancer.server.port=8265"
      - "m3tal.stack=media"
    networks:
      - proxy
    env_file:
      - ../../.env
    restart: unless-stopped

  glances:
    image: nicolargo/glances:latest
    container_name: glances
    pid: host
    command: glances -w
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.glances.rule=Host(`stats.${DOMAIN:?DOMAIN not set — run from repo root with .env}`)"
      - "traefik.http.services.glances.loadbalancer.server.port=61208"
      - "m3tal.stack=media"
    networks:
      - proxy
    env_file:
      - ../../.env
    environment:
      - HOMEPAGE_ALLOWED_HOST=*
      - NODE_TLS_REJECT_UNAUTHORIZED=0
    dns:
      - 8.8.8.8
      - 1.1.1.1
    restart: unless-stopped

  prowlarr:
    image: lscr.io/linuxserver/prowlarr
    container_name: prowlarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Denver
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9696"]
      interval: 60s
      timeout: 10s
      retries: 3
    volumes:
      - ${CONFIG_DIR:?CONFIG_DIR not set — run from repo root with .env}/prowlarr:/config
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}:/mnt
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/downloads:/downloads
      - ${DATA_DIR:?DATA_DIR not set — run from repo root with .env}/media:/media
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.prowlarr.rule=Host(`prowlarr.m3tal-media-server.xyz`)"
      - "traefik.http.services.prowlarr.loadbalancer.server.port=9696"
      - "m3tal.stack=media"
    networks:
      - proxy
    env_file:
      - ../../.env
    restart: unless-stopped

  flaresolverr:
    image: ghcr.io/flaresolverr/flaresolverr
    container_name: flaresolverr
    environment:
      - LOG_LEVEL=info
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.flaresolverr.rule=Host(`flaresolverr.m3tal-media-server.xyz`)"
      - "traefik.http.services.flaresolverr.loadbalancer.server.port=8191"
      - "m3tal.stack=media"
    networks:
      - proxy
    env_file:
      - ../../.env
    restart: unless-stopped

networks:
  proxy:
    external: true
